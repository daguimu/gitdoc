# ===================================
# GitDoc Configuration
# ===================================

# --- GitHub OAuth (Required) ---
# Create at: GitHub Settings → Developer settings → OAuth Apps
# Homepage URL: http://localhost:8080
# Callback URL: http://localhost:8080/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
OAUTH_STATE_SECRET=replace_with_a_long_random_secret

# --- Service ---
# Public URL for OAuth callback redirect. Change to your domain in production.
FRONTEND_URL=http://localhost:8080

# Host port mapping
SERVER_PORT=8080

# Timezone
TZ=Asia/Shanghai

# --- Mirror Acceleration (Optional) ---
# Default: official sources. Users in China can uncomment the lines below:
# NODE_IMAGE=docker.1ms.run/node:22-alpine
# PYTHON_IMAGE=docker.1ms.run/python:3.12-slim
# NPM_REGISTRY=https://registry.npmmirror.com
# PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
